extends ../layouts/main-layout.pug

block content

    .row
        .col-md-4 
            img(src=`/img/${article.imageUrl}` alt=`${article.description}`).img-fluid
        .col-md-8.pl-4
            h2 #{article.name}
            hr
            p #{article.description}
            
